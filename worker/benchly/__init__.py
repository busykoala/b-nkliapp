"""Benchly worker application package.

The command modules in ``worker/`` orchestrate jobs.  Durable data access and
feature-specific validation live here so the worker is maintained like the web
application rather than as a collection of deployment scripts.
"""


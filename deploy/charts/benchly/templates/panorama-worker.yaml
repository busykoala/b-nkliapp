apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-panorama
spec:
  replicas: 1
  strategy: {type: Recreate}
  selector:
    matchLabels: {app.kubernetes.io/instance: {{ .Release.Name }}, app.kubernetes.io/component: panorama}
  template:
    metadata:
      labels: {app.kubernetes.io/instance: {{ .Release.Name }}, app.kubernetes.io/component: panorama}
    spec:
      automountServiceAccountToken: false
      nodeSelector: {kubernetes.io/hostname: {{ .Values.nodeName }}}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        fsGroupChangePolicy: OnRootMismatch
      containers:
        - name: panorama
          image: {{ .Values.images.worker }}
          imagePullPolicy: IfNotPresent
          args:
            - panorama-worker
            - --database
            - /data/benchly.sqlite
            - --terrain-dir
            - /data/terrain-cache-v1/swissalti3d
            - --near-terrain-dir
            - /panorama/model/terrain/10m
            - --regional-terrain-dir
            - /panorama/model/terrain/90m
            - --cache-dir
            - {{ ternary "/panorama" "/data/panorama-cache-v1" .Values.panoramaStorage.enabled | quote }}
            - --processes
            - {{ .Values.workers.panoramaProcesses | quote }}
            - --limit
            - {{ .Values.workers.panoramaBatchSize | quote }}
          env:
            - {name: HOME, value: /tmp}
            - {name: TMPDIR, value: /tmp}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          resources:
{{ toYaml .Values.workers.panoramaResources | indent 12 }}
          volumeMounts:
            - {name: data, mountPath: /data}
{{- if .Values.panoramaStorage.enabled }}
            - {name: panorama, mountPath: /panorama}
{{- end }}
            - {name: temporary, mountPath: /tmp}
      volumes:
        - {name: data, persistentVolumeClaim: {claimName: {{ .Release.Name }}-data}}
{{- if .Values.panoramaStorage.enabled }}
        - {name: panorama, persistentVolumeClaim: {claimName: {{ .Release.Name }}-panorama}}
{{- end }}
        - name: temporary
          emptyDir: {sizeLimit: 2Gi}

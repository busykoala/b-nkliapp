DATABASE_PATH=./data/benchly.sqlite
CONTRIBUTOR_SECRET=replace-with-at-least-32-random-characters
RATE_LIMIT_SECRET=replace-with-at-least-32-random-characters
ADMIN_PASSWORD_HASH=
ADMIN_SESSION_SECRET=replace-with-at-least-32-random-characters
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Worker-only; never expose this credential through NEXT_PUBLIC_* variables.
INFERENCE_BASE_URL=http://localhost:8080
INFERENCE_API_KEY=
BENCHLY_VISION_MODEL=benchly-vision
# Keep false until the 100-location benchmark meets every acceptance threshold.
BENCHLY_AI_LABELS_ENABLED=false
